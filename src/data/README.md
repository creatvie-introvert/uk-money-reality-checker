# Data layer

The M1 data layer is reserved for validated release data and its provenance. Source spreadsheets must enter through an explicit importer and schema/release checks before typed loaders expose data for the M2 engine.