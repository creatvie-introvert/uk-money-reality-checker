# Release boundary

Milestone 0 establishes lint, strict type checking, unit tests, browser smoke tests and production builds. CI does not claim a data-validation gate exists yet.

The next data milestone should add a `data:validate` command and make release-status validation a required CI check before generated data can be used publicly.