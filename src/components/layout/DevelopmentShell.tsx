export function DevelopmentShell() {
  return (
    <main className="shell">
      <section className="shell__panel" aria-labelledby="page-title">
        <p className="shell__eyebrow">UK Money Reality</p>
        <h1 id="page-title">The rebuild foundation is active.</h1>
        <p className="shell__copy">
          The production application is being rebuilt with a documented data pipeline. The framework-independent calculator engine is planned for M2.
        </p>
        <p className="shell__status">Milestone 0 · Engineering foundation</p>
      </section>
    </main>
  );
}